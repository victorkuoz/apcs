n/* APCS 2018-02-p3 支點切割 */
#include <iostream>
#include <vector>

using namespace std;

#define acceralateIO(x) ios::sync_with_stdio(x); cin.tie(x); cout.tie(x);

int N, K;
vector<int> p, prefixSum, suffixSum;

int main () {
    acceralateIO(0);
    cin >> N >> K;

    p.resize(N+1);
    for (int i = 0; i < N; i++)

    return 0;
}